# S4 — Scenarios + Phase-Embedded Integration

**Date:** 2026-05-26
**Spec:** spec/spec_888_storm_framework.md (§4, §8, §17, §18, §19, §10 S4)
**Runner:** S4 implementation session

---

## 1. 11 scenarios created

Location: `~/.claude/skills/888/storm/scenarios/`

| File | Size | required_methods | selection_rules | detection_mode |
|---|---|---|---|---|
| T1_feature_intent.md | 4248 B | [39, 11, 34, 20, 42] (5) | 3 | keyword |
| T2_agent_create.md | 3955 B | [34, 35, 11, 17, 4] (5) | 2 | keyword |
| T3_agent_edit.md | 3667 B | [20, 34] (2) + cross-impact tracing | 2 | keyword |
| T4_comparator.md | 4524 B | [33, 36] (2) + rigor gates | 2 | keyword |
| T5_improvement.md | 3498 B | [42, 15, 11] (3) | 2 | keyword |
| T6_doubt_resolution.md | 3205 B | [41, 40, 39] (3) | 2 | keyword |
| T7_bug_fix.md | 3551 B | [35, 40] (2) | 2 | event |
| T8_merge.md | 3390 B | [34, 42, 17] (3) | 2 | keyword |
| T9_stuck_signal.md | 3613 B | [50, 15, 11] (3) | 2 | keyword |
| T10_destructive_op.md | 3567 B | [] (double-confirm protocol) | 3 | event |
| T11_regression_detected.md | 4036 B | [40, 35, 36, 50] (4) | 2 | automatic |

All 11 frontmatters contain required keys: `trigger_id`, `detection_keywords`, `required_methods`, `selection_rules` (≥2), `max_optional_selected`, `block_until`, `closes_gap`.

All `required_methods` IDs cross-checked against `embedded/elicitation-methods.csv` (50 valid IDs 1-50). **0 invalid IDs.**

Note on T7 `required_methods`: spec §4 row T7 says "#35 + forced taxonomy if N≥5" — added #40 (5 Whys) to the required list to align with `_TRIGGER_REQUIRED_METHODS["T7"]` in storm-orchestrator.py (which already had `[35, 40]`). This matches the consumer contract.

Note on T7 spec field deviation: §4 says the artifact is `_storm_audit/patch-counter.json` + `spec/taxonomy_<scope>.md`. The scenario's `required_artifact` is set to `spec/taxonomy_{slug}_storm.md` because that's what `code-gate.sh` blocks against; the counter JSON is a side-effect of `patch-counter.sh`, not the scenario artifact.

Note on T11 spec field deviation: §4 row T11 had `required_methods: #40, #35, #36, #50` — matches exactly.

---

## 2. Phase-embedded edits (7 files)

| # | File | Applied | Insertion section | grep `storm-orchestrator.py` |
|---|---|---|---|---|
| 1 | `~/.claude/skills/888/vendor/bmad-builder/src/skills/bmad-agent-builder/build-process.md` | YES | New `## Phase 4.5: Adversarial Review` between Phase 4 and Phase 5 | 2 occurrences PASS |
| 2 | `~/.claude/skills/888-persona-analyst/SKILL.md` | YES | End of `## Phase-gate check (handoff к architect)` section, before final `---` separator | 2 occurrences PASS |
| 3 | `~/.claude/skills/888-persona-architect/SKILL.md` | YES | Start of `## Что делать при вызове` (Phase 2 entry) | 2 occurrences PASS |
| 4 | `~/.claude/skills/888-persona-implementer/SKILL.md` | YES | After `## Что это за фаза`, before `## Field 0 — Severity classification` (Phase 2.5 entry) | 2 occurrences PASS |
| 5 | `~/.claude/skills/888-persona-qa/SKILL.md` | YES | Start of `## Что делать при вызове` (Phase 3 entry) | 2 occurrences PASS |
| 6 | `~/.claude/skills/888-persona-improver/SKILL.md` | YES | Start of `## Что делать при вызове`, before `### Mode A` (Phase 5 entry) | 2 occurrences PASS |
| 7 | `~/.claude/skills/bmad-auto-dev/SKILL.md` | YES | After `### Stage 4 — create-story` AND inside `#### Stage 6.pass` before merge | 4 occurrences PASS (T1 + T8) |

Pattern used (per S4 instructions): `### Storm Framework — <TX> (888 storm v1.0)` heading with HEADLESS_MODE and interactive invocation blocks + output / block condition note. No existing content was deleted or rewritten.

No target files were missing.

---

## 3. 888 SKILL.md update

File: `/home/server/.claude/skills/888/SKILL.md`
Section added: `## Storm Framework (888 storm v1.0 — added 2026-05-26)`
Position: After the file header / opening blockquote, before `## 1. Что делать при вызове`.
Contents: 1-paragraph what storm is (3-layer enforcement) + table of 11 trigger IDs with names + descriptions + pointers to scenarios/manifest.json/orchestrator/spec + dispatcher non-bypass note.

grep `## Storm Framework` (case-insensitive): 1 match — PASS.

---

## 4. YAML validation results

Method: `re.match(r'^---\n(.*?)\n---', text, re.DOTALL)` + `yaml.safe_load` via `/home/server/bmad-orchestrator/.venv/bin/python3` (PyYAML 6.0.3).

**11/11 PASS.** All required keys present (`trigger_id`, `detection_keywords`, `required_methods`, `selection_rules`, `max_optional_selected`, `block_until`, `closes_gap`). All `required_methods` IDs ∈ valid set 1-50 from `elicitation-methods.csv`. All `selection_rules` have ≥2 entries.

---

## 5. Validation tests

| # | Test | Result |
|---|---|---|
| 1 | YAML parse all 11 scenarios | **PASS** 11/11 |
| 2 | grep `storm-orchestrator.py` in 7 phase-embedded files | **PASS** 7/7 (counts: 2,2,2,2,2,2,4) |
| 3 | grep `## Storm Framework` in `~/.claude/skills/888/SKILL.md` (case-insensitive) | **PASS** 1 match |
| 4 | Imitation smoke test: `echo '{"prompt":"хочу внедрить foo","session_id":"s4-smoke"}' \| bash ~/.claude/hooks/intent-detector.sh` | **PASS** exit 0, T1 trigger emitted with correct context inject |

Additional direct classifier test: `python3 ~/.claude/skills/888/storm/intent-classifier.py "хочу внедрить foo"` → `{"triggers": ["T1"], "scenarios": ["T1_feature_intent.md"], ...}` PASS.

---

## 6. Cross-check classifier vs scenario keywords

Manually verified that classifier regex patterns (which use `\s+` between word boundaries) DO match each scenario `detection_keywords` literal (with single space). Initial naive overlap test using `re.escape(kw)` against classifier raw pattern produced false MISMATCH for T2/T3; re-tested by actually applying the regex to the keyword strings:

| Trigger | Scenario keywords that classifier matches | Gaps |
|---|---|---|
| T1 | 7/10 (хочу внедрить, добавим, новая фича, новую фичу, новый функционал, build feature, хочу добавить, let's add, implement, feature request) | "let's add", "feature request", "новый функционал" not in classifier pattern — but classifier has `implement`, `build feature`, `новую фичу` matching — sufficient overlap. |
| T2 | 7/8 (all except "scaffold agent") | "scaffold agent" not in classifier T2 pattern — minor gap, low impact. |
| T3 | 7/7 all match | none |
| T4 | 9/9 all match | none |
| T5 | 6+/8 (most match; рефакторинг has no exact classifier match but `refactor`/`оптимизирую` cover the family) | minor synonym gaps |
| T6 | 9/9 all match | none |
| T7 | event-mode, classifier pattern is fallback only | OK |
| T8 | 6/6 match | OK |
| T9 | 8/8 match | OK |
| T10 | event-mode | OK |
| T11 | automatic + fallback | OK |

**Notable mismatches:**
- T2 scenario lists "scaffold agent" but classifier pattern doesn't include "scaffold". Considered low-risk: BMB phase-embedded `Phase 4.5` is the primary enforcement path for agent creation in headless / auto mode; intent-detector is the keyword path only.
- T1 has 3 keywords ("let's add", "feature request", "новый функционал") not in classifier pattern. Same low-risk reason: phase-embedded T1 in analyst/architect/bmad-auto-dev is primary.

**Recommendation (out of scope for S4):** in a follow-up patch, optionally add `scaffold\s+agent` to T2 classifier pattern and `новый\s+функционал|feature\s+request|let.s\s+add` to T1 pattern. NOT done in S4 per "do NOT modify any python module from S2" rule.

---

## 7. Deviations / decisions made

1. **T7 required_methods uses [35, 40]** instead of just [35] per §4 — this matches the consumer contract in `storm-orchestrator.py` `_TRIGGER_REQUIRED_METHODS["T7"]` which was already wired during S2.
2. **T3 required_methods uses [20, 34]** instead of "#20 ADR, #34, cross-impact tracing" — cross-impact tracing is added as a mandatory non-method section in the scenario body (not a method ID) since it's not in `methods.csv`.
3. **T4 required_methods uses [33, 36]** plus 3 mandatory non-method rigor gates (code-grounding, dedup-grep, counter-example gate) per §4 row T4 + memory feedback_comparator_shallow_template. The rigor gates are added as scenario-body sections, not method IDs.
4. **T7 detection_mode = event** with empty `detection_keywords: []` per S4 hard rules (event-driven not keyword-driven).
5. **T10 detection_mode = event**, empty required_methods, body explains double-confirm protocol per S4 hard rules.
6. **T11 detection_mode = automatic** (R1-R5 detectors); classifier also has a keyword fallback pattern but primary path is auto-detection.
7. **architect phase-embedded** does NOT reference T20 (T20 is not defined in this version per S4 instructions — note added in scenario body explicitly).
8. **bmad-auto-dev** has 2 storm sections (T1 at Stage 4 = story-start, T8 inside Stage 6.pass = pre-merge) → 4 grep matches total.

---

## 8. What was NOT done (per instructions)

- No commit created.
- No Python modules from S2 modified.
- No hooks from S1/S1.5 modified.
- No pip packages installed (used existing `/home/server/bmad-orchestrator/.venv/bin/python3` for PyYAML 6.0.3).
- `embedded/elicitation-methods.csv` unchanged.
- Existing sections in target SKILL.md / build-process.md files preserved (only ADDed new sections).
